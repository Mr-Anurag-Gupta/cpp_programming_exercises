/**
 * Modify the program in exercise 15.4 to count the number of words which
 * start with the character 's'.
*/

#include <iostream>
#include <cstdint>
#include <string>

int main()
{
    std::string str = std::string("Same old, same old");
    const char ch = 's';

    uint16_t no_of_words = 0;

    while ((n = str.find("It", n)) != std::string::npos)
    {
        n += 2;
        it_count += 1;
    }

    // reset n
    n = 0;

    return (0);
}